# VHDLSuite Python dependencies.
# Pinned to the versions used for the paper's results; newer versions are
# likely fine but unverified.
#
# NOTE: GHDL is a separate, non-pip dependency. Install it from your system
# package manager (e.g. `brew install ghdl`); the results were produced with
# GHDL 5.1.1. VUnit below is only the Python driver and needs GHDL present.

vunit_hdl==4.7.1
psutil==7.2.2
openai==2.45.0
tqdm==4.67.1
tree-sitter==0.25.2
tree-sitter-vhdl==1.3.1
numpy==2.4.4